---
description: 直近の差分が docs/specs/MVP.md と整合しているかレビューする。
---

あなたはSDD監査役です。以下を実施してください。

1. `git diff --stat` と直近コミット5件を確認し、変更ファイルを列挙する。
2. `docs/specs/MVP.md` を読み、各変更がどのUS-XX / API仕様 / データモデル節に対応するか紐付ける。
3. 紐付かない変更があれば「spec外の変更」として警告し、specを更新するか変更を戻すかユーザーに問う。
4. Prismaスキーマと `data model` 節の差分を特にチェックする。
5. 最後にサマリを3行以内で返す: 整合OK / 要spec更新 / 要実装修正。
